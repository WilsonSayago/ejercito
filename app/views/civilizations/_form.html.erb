<%= form_with(model: civilization, local: true) do |form| %>
  <% if civilization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(civilization.errors.count, "error") %> prohibited this civilization from being saved:</h2>

      <ul>
      <% civilization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.select :name, Civilization::CIVILIZACIONES, { include_blank: true }, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
